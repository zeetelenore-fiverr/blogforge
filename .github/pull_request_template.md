## What this changes

<!-- One or two sentences. -->

## Why

<!-- The problem, not the patch. -->

## Checked

- [ ] `npx tsc --noEmit` is clean
- [ ] `npm run build` succeeds
- [ ] I clicked through the affected screens
- [ ] Works with no API key configured (or degrades clearly if it cannot)
